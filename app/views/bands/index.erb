<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <title>This is the band index</title>
    </head>
    <body>
        <h1>List Bands</h1>
            <ul>
                <% Band.all.each do |band| %>

                    <li><a href="/bands/<%=band.id%>"><%= band.name %></a></li>

                <%end%>
            </ul>

        <a href="/bands/new">Create New Band</a>
        <a href="/">Home Page</a>
    </body>
</html>
